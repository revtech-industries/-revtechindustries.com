Optional background images (Artemis Halo aesthetic).

If you add PNGs here, name them for use in CSS, e.g.:
  bg-floating-halo.png   — premium floating ring / halo
  bg-radial-cyan.png     — radial gradient cyan
  bg-soft-minimal.png    — soft minimalist
  bg-halo-pattern.png    — repeating subtle halo rings

The site already uses CSS gradients for the same look; these images
can be used to override or layer (e.g. in .gallery or .gallery-hero-label)
for a richer background.
